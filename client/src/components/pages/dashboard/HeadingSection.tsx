import { Typography } from "@mui/material";

export const HeadingSection = () => {
  return (
    <>
      <Typography variant="h4">Overview</Typography>
      <Typography variant="caption" ml={0.5}>
        Overview uses generated ride sharing data
      </Typography>
    </>
  );
};
