import { Typography } from '@mui/material';

export const HeadingSection = () => {
  return (
    <>
      <Typography variant="h4">Categories</Typography>
      <Typography variant="caption" ml={0.5}>
        Listing of categories and actions to modify them
      </Typography>
    </>
  );
};
